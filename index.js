const divide = function(a, b) {
    return a / b;
};
console.log(divide(2000, 100)); // Output: 5

const square = (num) => {
    return num * num;
};

const add = (num1,  num2) => {
    return num1 + num2;
};